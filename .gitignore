/config/*
!/config/formats.ts
# M12 fixed roster — the pre-registered team, must be tracked (see docs/BATTLE-FORMATS.md)
!/config/rosters/
!/config/rosters/*.txt
/logs/**/*
/test/modlogs
/test/replays/*.html
/node_modules
/eslint-cache
/server/chat-plugins/private
/server/chat-plugins/private.js
/server/chat-plugins/*-private.js
npm-debug.log
.eslintcache
.eslintcache-no-types
/tools/set-import/sets
databases/*.db*
*.o
*.o.dSYM
# Typescript build artifacts
.*-dist/
tools/set-import/importer.js
tools/modlog/converter.js
*.tsbuildinfo
**/sourceMaps

!test/**/*.js
tools/modlog/**/*.js
tools/set-import/*.js

# visual studio live share
.vs

# boilerplate #
###############

.DS_Store

# editor temp files
*~
.idea/

tmp/**/*
*.lock
*.DS_Store
*.swp
*.out
*.pyc
*.pyo
.installed.cfg

downloads
dist

# egg related
develop-eggs
*.egg-info
*.egg
*.EGG
*.EGG-INFO
src/*.egg-info
eggs
fake-eggs

# django
local_settings.py

# SCM
.hg
.bzr
.svn

pip-log.txt
# redis dumps
*.rdb

# ML training artifacts
__pycache__/
# Intermediate/sweep checkpoints stay local — only the machine that trained a
# run needs them. Final checkpoints ARE committed (~2MB each) so any machine
# can pick up evals, MCTS runs and fine-tunes without a bulk transfer.
models/**/checkpoints/**/*.pt
!models/**/checkpoints/**/*_final.pt
models/**/checkpoints/**/*.pkl
models/q_learning/qtable.pkl
output/

# Metamon (M2.5 BC pretraining) — vendored install + downloaded dataset
vendor/
data/metamon_cache/

# M5.5 human-replay data pipeline (scraped logs + derived trajectories)
data/replays/
data/replay_trajs/

# M8 Phase 2 value-target datasets (collect_value_data.py .npz shards)
data/value_targets/
models/transformer/checkpoints/m32/train.log

# Showdown ladder-bot credentials (M6) — never commit.
# M9 uses one account (and so one file) per A/B arm: showdown_login_m9p3ctl.txt
# etc. See docs/EVALUATION-METHODOLOGY.md.
showdown_login.txt
showdown_login_*.txt

# Analysis outputs from scripts/mine_gen1ou_*.py (regenerable)
results/gen1ou_*.json
